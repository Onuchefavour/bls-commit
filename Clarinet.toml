[project]
name = "bls-commit"
authors = ["BLS Commitment Team"]
description = "A BLS-based commitment and verification smart contract platform"
telemetry = false
requirements = []
[contracts.bls-commitment]
path = "contracts/bls-commitment.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
